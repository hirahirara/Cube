const map =
{
    ground: {x:0, y:1000, width:1000, height:1},

    groundUpdate: function ()
    {
        //vctx.strokeStyle = color.ground;
        //vctx.strokeRect(this.ground.x, this.ground.y, this.ground.width, this.ground.height);
        //if(collision(player, this.ground)) collisionObject.divisionNormal(player, this.ground);
    }
}